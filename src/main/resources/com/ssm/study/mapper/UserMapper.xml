<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.study.mapper.UserMapper">

    <select id="findAll" parameterType="string" resultType="com.ssm.study.entity.Girl">
        select * from girl where name like '%${value}%'
    </select>
</mapper>