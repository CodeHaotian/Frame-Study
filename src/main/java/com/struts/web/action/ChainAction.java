package com.struts.web.action;

import com.opensymphony.xwork2.ActionSupport;

/**
 * 用于转发到CaptchaResult
 * @Author：Haotian
 * @Date：2019/5/17 17:36
 */
public class ChainAction extends ActionSupport {
    /**
     * ActionSupport为Action的实现类
     * 其中的execute()方法默认返回success
     * 所以继承后可以什么也不写
     */
}
