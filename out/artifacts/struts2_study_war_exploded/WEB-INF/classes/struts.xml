<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
        "http://struts.apache.org/dtds/struts-2.5.dtd">


<struts>
    <!--配置常量-->
    <!--1.配置访问的后缀名-->
    <constant name="struts.action.extension" value="php"></constant>

    <!--配置开发模式-->
    <constant name="struts.devMode" value="true"></constant>
    <!--
    package:表示包
            name:包名，在struts.xml文件不能有相同的包名，包名是唯一的
            extends:继承，固定struts-default
            abstract:抽象包，被用于继承，一般在抽象包里不声明action

    action:动作
             name:相当于Servlet的映射路径(@WebServlet)
             class:处理请求的类，相当于一个人Servlet类
             method:处理请求的方法
             public:
                    1.pub
    result:结果，写返回的jsp页面
    -->
    <package name="p1" extends="struts-default">
        <action name="hello" class="com.yileaf.web.action.HelloAction" method="sayHello">
            <result name="success">/success.jsp</result>
        </action>
    </package>

</struts>